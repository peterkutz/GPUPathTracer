#include "material.h"

/*
__host__ __device__
Material makeEmptyMaterial() {
	Material material;
	material.diffuseColor = make_float3(0,0,0);
	material.emittedColor = make_float3(0,0,0);
	material.specularColor = make_float3(0,0,0);
	material.hasTransmission = false;
	material.medium.refractiveIndex = 0;
	material.medium.absorptionAndScatteringProperties.absorptionCoefficient = make_float3(0,0,0);
	material.medium.absorptionAndScatteringProperties.reducedScatteringCoefficient = 0;
	return material;
}
*/

